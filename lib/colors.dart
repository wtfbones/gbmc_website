import 'package:flutter/material.dart';

class AppColors {
  Color get primary => ColorPalette.headerBlack;

  Color get error => ColorPalette.amaranth;
}

class ColorPalette {
  ColorPalette._();
  static const headerBlack = Color(0xFF021E1F);

  static const amaranth = Color(0xffe63f5c);
}
